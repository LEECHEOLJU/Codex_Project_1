# PRD: 멀티 SIEM 연동형 Jira 대체 보안관제 티켓 플랫폼

## 1. 문서 개요
- **문서 목적**: 기존 Jira 기반 보안관제 업무를 대체할 수 있는 자체 플랫폼의 제품 요구사항을 정의한다.
- **핵심 방향성**:
  1) 다양한 SIEM(Splunk, Microsoft Sentinel, 로그프레소 등) 이벤트를 안정적으로 수집
  2) 고객사/SIEM 추가·변경 시 **코드 수정 없이 UI 기반 설정(No-Code Ops)**
  3) 보안분석가의 티켓 분석/조치/고객 통지를 단일 워크플로우로 자동화
  4) 장기적으로 AI 보안분석 및 SOAR 연동까지 확장 가능한 구조

---

## 2. 배경 및 문제 정의
현재 운영 모델은 SIEM 이벤트를 Jira 티켓으로 연계해 분석가가 처리하는 구조다. 하지만 다음 문제가 존재한다.

- 고객사별 필드/워크플로우 차이로 인해 설정·운영 복잡도 증가
- SIEM별 이벤트 포맷 차이로 인한 매핑 비용 증가
- 플랫폼 변경(신규 고객사/신규 SIEM) 시 코드 레벨 수정 리스크
- 대량 이벤트 처리 시 유실/중복 통제, 추적성, 멱등성 보장 필요
- 보안관제 현업 기준으로 Jira는 보안 분석 UX 최적화가 제한적

**제품 목표**는 "Jira를 대체하는" 수준이 아니라, 보안관제 업무에 최적화된 **티켓 중심 SOC 운영 플랫폼**을 구축하는 것이다.

---

## 3. 제품 비전 / 목표 / 비목표

### 3.1 비전
"멀티테넌트 환경에서 다양한 SIEM를 무중단 연동하고, 분석·조치·보고까지 자동화하는 차세대 AI SOC 티켓 플랫폼"

### 3.2 정량 목표 (초기 버전)
- 이벤트 수집 성공률: **99.99% 이상**
- 중복 티켓 생성률: **0.1% 이하** (멱등성 키 기반)
- 이벤트→티켓 생성 지연: P95 **30초 이내**
- 일 처리량: 기본 **500건/일 이상**, 버스트 허용 10배
- 신규 고객사 온보딩(연동 설정 완료까지): **1일 이내**

### 3.3 비목표 (MVP 범위 외)
- SIEM 자체 기능(규칙엔진/검색엔진) 대체
- EDR/NDR 풀스택 대체
- 완전 무인 대응(분석가 승인 없는 전면 자동 조치)

---

## 4. 사용자 및 역할 정의

### 4.1 사용자 페르소나
1. **SOC 1차 분석가**
   - 신규 티켓 분류, 오탐/실탐 판별, 초동 조치, 보고서 작성
2. **SOC 관리자(팀장/운영자)**
   - 워크플로우/필드/우선순위/SLA 정책 관리, 품질/성과 모니터링
3. **플랫폼 관리자(시스템/보안관리자)**
   - 고객사 생성, 계정·권한 관리, SIEM 커넥터 설정
4. **고객사 담당자**
   - 티켓 결과 수신(메일/포털), 조치 이력 확인

### 4.2 권한 체계 (RBAC + 테넌트 스코프)
- **Global Admin**: 전체 테넌트 관리
- **Tenant Admin**: 해당 고객사 설정 관리
- **Analyst**: 티켓 처리 및 분석 필드 편집
- **Auditor/Viewer**: 읽기 전용, 감사 추적 확인

권한은 `테넌트 범위 + 리소스 + 액션(Create/Read/Update/Delete/Approve)` 조합으로 정책화한다.

---

## 5. 핵심 시나리오 (End-to-End)

### 5.1 표준 처리 흐름 (고객사 이벤트 → 분석 → 통지)
1. 고객사 SIEM에서 이벤트/알람 발생
2. SIEM이 플랫폼 Webhook 엔드포인트로 POST 전송
3. 수신 서비스가 원본 payload를 메시지 큐에 적재 후 즉시 202 응답
4. 워커가 메시지를 읽어 커넥터 규칙에 따라 정규화
5. 동적 필드 매핑 엔진으로 티켓 필드 변환
6. 멱등성 키 검증 후 티켓 생성/업데이트(upsert)
7. 분석가가 3-Pane 화면에서 분석 후 상태 전이
8. 상태 전이 트리거로 고객 통지 템플릿 발송
9. 분석 결과와 처리 로그는 감사/지식베이스에 저장

### 5.2 Splunk 특화 흐름 (중요)
- Splunk 웹훅은 경우에 따라 결과 전체가 아닌 식별자(sid/results_link) 중심 전달 가능
- 플랫폼은 sid 기반으로 Splunk REST API를 역조회해 전체 결과를 보강 수집
- sid를 외부 고유키(external_event_key)로 사용해 중복 방지

---

## 6. 제품 기능 요구사항

## 6.1 멀티 SIEM 수집
- 지원 대상: Splunk, Sentinel, 로그프레소 (확장형 커넥터 구조)
- 입력 방식:
  - Webhook Push
  - API Pull (필요 시)
- 기능:
  - 커넥터별 인증(토큰/OAuth/API Key)
  - 재시도, 백오프, 서명 검증, IP Allowlist
  - 원본 payload 보관(법적/감사 목적)

## 6.2 동적 필드 매핑 엔진 (No-Code 핵심)
- UI에서 고객사별 "소스 필드 → 티켓 필드" 드래그&드롭 매핑
- JSONPath/JMESPath 기반 추출식 지원
- 변환 함수: 타입변환, 정규식 추출, 값 매핑, 기본값, 조건식
- 버전 관리: 초안/배포/롤백
- 테스트 모드: 샘플 payload로 즉시 검증

## 6.3 티켓/워크플로우 엔진
- 커스텀 상태 정의(예: New → Analyzing → Pending Customer → Resolved)
- 상태 전이 조건(필수 필드 입력, 승인자 필요 여부)
- SLA 타이머(예: New 상태 30분 초과 시 에스컬레이션)
- 자동 액션: 메일/웹훅/슬랙/Teams 알림

## 6.4 분석가 콘솔 (3-Pane UX)
- 좌측: 티켓 리스트(심각도/SLA/고객사/정렬/대량선택)
- 중앙: 상세(원본 로그, 정규화 데이터, AI 요약)
- 우측: 액션(상태변경, 플레이북 실행, 커스텀 필드 입력)
- 단축키/빠른 처리(대량 triage)

## 6.5 관리자 콘솔 (No-Code 운영)
- 고객사 생성/비활성화
- SIEM 연동 추가 마법사 (연결 테스트 포함)
- 필드 스키마 관리(시스템 필드/커스텀 필드)
- 워크플로우 디자이너(UI state machine)
- 템플릿 관리(고객 통지 메일, 내부 알림)

## 6.6 계정/권한/감사
- SSO(SAML/OIDC) + MFA
- 세분화 RBAC, 테넌트 격리
- 모든 설정 변경/상태변경 감사로그(누가, 언제, 무엇을)

## 6.7 AI 보안 분석 (단계적 도입)
- 1단계: 요약/초동 분석 초안 작성
- 2단계: 유사 사건 추천, 오탐 가능성 스코어
- 3단계: 대응 가이드 생성 + 반자동 필드 업데이트
- 안전장치: Human-in-the-loop, 프롬프트/응답 감사 저장

---

## 7. 비기능 요구사항 (NFR)

## 7.1 안정성/무결성
- At-least-once 전달 전제 + 멱등성 처리
- 큐 영속화, DLQ(Dead Letter Queue), 재처리 파이프라인
- 장애 복구 목표: RPO 5분 이하, RTO 30분 이하

## 7.2 성능/확장성
- 수집 계층 수평 확장 가능
- 워커 오토스케일링(큐 길이/처리지연 기반)
- DB 인덱스 최적화(JSONB + GIN 포함)

## 7.3 보안/컴플라이언스
- 전송구간 TLS, 저장구간 암호화
- 비밀정보 관리(Secret Vault)
- 테넌트 단위 Row-Level Security
- 개인정보 마스킹/보존기간 정책

## 7.4 운영성
- 메트릭/로그/트레이스 표준화(OpenTelemetry)
- 운영 대시보드: 수집성공률, 지연, 실패율, SLA 위반률
- 알람 정책: 임계치 기반 on-call 연결

---

## 8. 시스템 아키텍처

## 8.1 논리 아키텍처
1) **Ingestion Layer**: API Gateway + Webhook Receiver
2) **Messaging Layer**: Queue/Broker + DLQ
3) **Processing Layer**: Normalizer + Mapping Engine + Workflow Engine
4) **Data Layer**: PostgreSQL(트랜잭션), Object Storage(원본 보관), Cache
5) **Application Layer**: Ticket Service, Admin Service, Notification Service
6) **AI Layer**: LLM Gateway, RAG/Vector Store(선택)
7) **Presentation Layer**: Analyst UI, Admin UI, Customer Portal(선택)

## 8.2 배포 옵션 (Vercel + Supabase 포함)

### 옵션 A: 빠른 구축형
- Frontend: Vercel (Next.js)
- DB/Auth: Supabase(PostgreSQL, Auth, RLS)
- 별도 Backend: FastAPI + Queue Worker (필수)
- 장점: 개발 속도 빠름, 초기 인프라 부담 낮음
- 주의: 대규모 비동기 처리/네트워크 통제는 별도 백엔드 설계 필요

### 옵션 B: 엔터프라이즈 운영형
- Frontend: 사내/클라우드 배포
- Backend: Kubernetes 기반 마이크로서비스
- DB: Managed PostgreSQL + 전용 메시지 브로커
- 장점: 고가용성/확장성/보안 통제 강화
- 단점: 구축·운영 비용 증가

---

## 9. 데이터 모델 (개념)

### 핵심 엔티티
- Tenant
- User / Role / Permission
- SIEMConnector
- MappingProfile (versioned)
- WorkflowDefinition / WorkflowTransition
- Ticket / TicketEvent / TicketComment
- NotificationRule / Template
- IngestionEvent (원본)
- AuditLog

### 핵심 키 정책
- `external_event_key` (SIEM별 고유 이벤트 키, 예: sid+result_id)
- 티켓 생성 시 `tenant_id + external_event_key` 유니크 제약
- 중복 수신 시 Insert 대신 Upsert

---

## 10. UI/UX 상세 설계

## 10.1 정보구조(IA)
- 대시보드
- 티켓
- 고객사 관리
- SIEM 연동 관리
- 필드/매핑 관리
- 워크플로우 관리
- 계정/권한
- 알림/템플릿
- 감사로그/리포트

## 10.2 주요 화면
1. **티켓 워크벤치(분석가)**
   - 대량 처리/필터 프리셋/심각도 우선 큐
2. **연동 설정 마법사(관리자)**
   - Connector 선택 → 인증정보 입력 → 샘플 수신 → 매핑 → 테스트 → 배포
3. **매핑 빌더**
   - 소스 JSON 트리 + 대상 필드 패널 + 변환식 에디터 + 미리보기
4. **워크플로우 디자이너**
   - 상태 노드/전이 조건/트리거 시각화 편집
5. **권한 정책 편집기**
   - 역할 템플릿 + 세부 권한 토글 + 범위(테넌트/리소스)

---

## 11. 운영 리스크 및 통제 전략

1. **로그 유실 리스크**
   - 원인: 네트워크 장애, 큐 장애, 역조회 지연
   - 대응: 재시도/백오프, 큐 영속화, DLQ 재처리, Pull TTL 모니터링

2. **중복 적재 리스크**
   - 원인: 웹훅 재전송, 큐 중복 전달
   - 대응: 멱등성 키 유니크 제약, Upsert, 중복 카운터 모니터링

3. **설정 오류 리스크(No-Code 오남용)**
   - 대응: 샌드박스 테스트, 변경승인(2인), 버전 롤백

4. **AI 오판 리스크**
   - 대응: 분석가 승인 전 자동 종결 금지, 근거 출처 표시, 품질평가 루프

---

## 12. 개발 로드맵 (제안)

### Phase 1 (MVP, 8~10주)
- 멀티테넌트 계정/RBAC
- Webhook 수집 + 큐 + 티켓 생성
- 기본 매핑 UI/워크플로우 UI
- 분석가 3-Pane 티켓 처리 UI
- 메일 통지

### Phase 2 (고도화, 6~8주)
- Splunk sid 역조회, Sentinel/로그프레소 커넥터 강화
- SLA/에스컬레이션, 감사 리포트
- 매핑 버전관리/롤백/테스트 자동화

### Phase 3 (AI + 자동화, 8주+)
- AI 요약/유사사건 추천/RAG
- SOAR 연동 플레이북
- 고객 포털/자가 조회 기능

---

## 13. 성공지표 (KPI)
- 이벤트 수집률, 처리지연, 중복률
- 티켓 TTA(착수시간), TTR(해결시간)
- 분석가 1인당 처리량
- SLA 위반률
- 자동화율(자동 채움 필드 비율, 자동 통지 성공률)
- 고객사 만족도(NPS/CSAT)

---

## 14. 오픈 이슈
1. 고객사별 데이터 보존기간/국가별 규제 요구 차이
2. SIEM별 API 제한(속도 제한, TTL 정책) 표준화 전략
3. 메일 외 채널(Slack/Teams/문자) 우선순위
4. AI 기능의 책임 범위(권고 vs 자동 실행)

---

## 15. 결론
본 PRD는 "SIEM 이벤트 수집 → 티켓 생성 → 분석가 처리 → 고객 통지"의 전체 가치사슬을 Jira 의존 없이 자체 플랫폼에서 실행하기 위한 기준 문서다. 핵심은 **No-Code 설정 기반 멀티테넌시 운영**, **유실/중복 방지 중심의 비동기 아키텍처**, **분석가 중심 UX**, **단계적 AI 내재화**다.

즉, 이 프로젝트의 본질은 단순한 티켓 시스템 개발이 아니라, 보안관제 업무를 표준화·자동화·지능화하는 운영체계(Operating System)를 만드는 것이다.
